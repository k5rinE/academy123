import java.util.*;
public class Test5 {
	public static void main(String[] args) {
		
		
		
		System.out.print("나이입력: ");
		Scanner sc = new Scanner(System.in);
		int age = sc.nextInt();
		double rate=0;
		if(age<=3 || age>=65) 
			rate = 0;
		else if(age>=20)
			rate = 1;
		else if(age>=14)
			rate=0.75;
		else if(age>=4)
			rate=0.5;

		System.out.println("나이: " + age + ",요금: "+(int)(cost*rate));
	}
	
	static int add(int age, double rate, int cost) {
		
	
		
		int cost=2500;
	
		double rate =0;
		int num = add(age, rate, cost);
		
		
		
		
		
		
		
		
		return add( age, rate, cost);
	}
}
		